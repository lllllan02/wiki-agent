# 缓存穿透

缓存穿透指请求的数据在缓存和数据库中都不存在，导致请求反复绕过缓存访问数据库。

常见应对方式：

- 对不存在的数据缓存空值，并设置较短的过期时间，减少重复查询；需要注意新数据写入后的缓存一致性。
- 使用布隆过滤器过滤一定不存在的键；布隆过滤器存在误判，仍需访问后端确认可能存在的键。
- 校验输入并限制异常请求频率。

缓存穿透与缓存击穿不同：后者通常指热点键过期后，大量并发请求同时访问数据库。

这是一份用于 L01 验证的示例笔记，可替换为自己的资料。
