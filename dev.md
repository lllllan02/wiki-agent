# 从零构建 Wiki Agent Harness：连续迭代路线

从一个最小 Agent Loop 开始，在同一个项目上依次建立工具系统、执行控制、状态与上下文管理、可靠写入、长任务恢复，最后扩展到后台运行与协作。

本路线按 Harness 的构建依赖组织。先建立一个可运行的基础，再在其上增加下一层机制；问题、故障案例与方案比较在相应阶段辅助理解，不要求每一轮都以提问或旧版失败开场。

## 1. 项目边界与学习方式

新建项目，初始形态为单用户、本地 Markdown、本地 Web 页面、一个支持工具调用的 EINO 模型适配器。建立一个小 Wiki，放入自己理解的真实笔记与资料，作为贯穿全程的工作对象。

最终通过本地 Web 界面使用 Agent，支持对话、任务进度、追加要求、暂停继续和变更审阅。另行建设 Agent 操作网页的能力，用于从网页观察、搜索、交互和收集资料。用户使用的 Web 界面与 Agent 控制的浏览器是两个独立模块，共用已有任务运行基础。

保持的用户任务：读取、搜索、整理资料、提出修改、编辑笔记、维护多个页面。任务随着所需工具逐步开放，教学顺序由运行基础的依赖决定。

- 始终在同一套代码上继续修改，不为每一课复制工程。
- 不要求先让旧版失败，也不要求每轮归档、报告、标签或分支。
- 每次看清楚已有基础、本次增加的机制及完成后的能力，再做必要验证。
- 早期机制可以简单，后续再系统化；不提前搭完所有框架。
- 已有能力持续保留，检查与验证按改动影响范围进行。
- 固定 Workflow、预定义角色的多 Agent、运行时动态委派都要实际实现并验收，按依赖逐步增加。后续加入动态能力时保留固定流程，支持分别运行与比较。
- 示例和故障实验按需使用；超时、幂等、恢复等机制需要实际验证其相关行为，可以直接在新实现上验证。
- 学习过程的记录是可选的；Agent 自身用于运行诊断和恢复的日志、状态与检查点是工程内容。

Claude Code 类系统作为工程能力参照。完成路线后，以声明的任务与环境中的评估结果判断可靠性，不用模块数量代替验证。

## 2. 先看整条构建主线

| 阶段 | 轮次 | 在已有系统上增加什么 | 阶段完成时的系统 |
|---|---|---|---|
| 建立核心 | L01–L04 | 循环 → 多工具 → 工具契约 → 执行器 | 有清楚职责划分的工具调用程序 |
| 控制执行 | L05–L09 | 状态与预算 → 超时、暂停与运行中输入 → 恢复 → 观测 → 评估 | 能暂停、调整方向、停止和诊断的只读 Agent |
| 可靠写入 | L10–L12 | 权限 → 变更提案与受控编辑 → 幂等核对 | 能在授权范围内可靠修改笔记 |
| 管理上下文 | L13–L15 | 输入组装 → 压缩 → 会话存储 | 能处理增长的上下文并恢复会话 |
| 管理长任务 | L16–L18 | 固定 Workflow 与动态计划 → 持久执行恢复 → 多文件隔离变更 | 能执行、恢复和交付多步修改 |
| 复用与扩展 | L19–L21 | 长期记忆 → Skills → Hooks | 能复用信息、流程并扩展运行行为 |
| 扩展执行方式 | L22–L26 | 后台与 Web 界面 → 网页交互与 MCP → 固定角色多 Agent → 动态委派与协作 → 调度 | 能通过网页使用 Agent、采集网页资料，并管理两种多 Agent 模式 |
| 验证交付 | L27–L28 | 质量与效率优化 → 综合验证与发布 | 在明确范围内可持续使用的完整系统 |

这里的顺序是教学安排。MCP、调度并不技术依赖多 Agent；把它们放在后面，是为了复用已经建立的执行控制。Hooks 后置，是先写清楚具体逻辑，再抽象扩展接口。

路线中设置三个可以开始实际使用的里程碑，不必等到 L28 才交付：

| 节点 | 可交付版本 | 使用范围 |
|---|---|---|
| L09 | 只读助手 | 搜索、读取、回答，并检查结果与来源 |
| L12 | 单文件编辑助手 | 预览并应用修改，处理重复请求和结果不确定；进程退出后的自动恢复留到 L17 |
| L18 | 核心版本 | 执行多步、多文件任务，核对并恢复中断执行 |

L19–L26 在核心版本上继续建设复用、后台和协作能力；本学习路线中 L24 的固定角色多 Agent 与 L25 的动态委派都是必做内容。里程碑可以先交付使用，但不代替后续学习验收。每个里程碑都做与当前能力匹配的质量、耗时和成本检查，补齐启动方式、使用边界及必要的备份说明；L27–L28 再进行系统优化与综合交付验证。

## 3. 第一轮：先把 Agent Loop 做出来

第一轮不预设你已经有 Agent。先讲明白并实现以下运行过程：

1. 接收一个用户请求，把它放进消息列表。
2. 把消息和一个工具定义交给模型。
3. 模型如果返回工具调用，程序识别工具名与参数。
4. 程序执行工具，把真实结果按调用标识放回消息列表。
5. 再次调用模型，直到得到最终回答，或达到基本停止上限。

输入示例：“读取 notes/cache.md，根据笔记解释缓存穿透。”

第一轮的代码只需要一个本地 Web 入口、一个消息列表、一个 EINO OpenAI 兼容模型构造、一个受目录限制的 read_note 工具和上述循环。当前实现使用 EINO ADK ChatModelAgent 与 Runner 承担循环，项目代码不再手写工具调用分支。

工具读取的目录限制、基本步数上限和异常退出从开始就有，但此时不需要设计权限策略引擎、完整预算系统或复杂恢复系统。后面在它们的基础上逐步深化。

完成后先确认能走通“请求 → 工具 → 结果 → 回答”。这时才进入第二轮，把单工具结构改成多工具注册表。

## 4. 逐轮建设清单

### L01：搭建最小 Agent Loop

从空项目开始，先建立一条真正能运行的模型—工具交互链路。

- [x] 准备本地 Web 入口、EINO OpenAI 兼容模型构造、一个消息列表和受 Wiki 目录限制的 read_note 工具。使用 EINO ADK ChatModelAgent 和 Runner 管理模型—工具循环；项目代码只负责创建 WikiAgent、传入历史消息和当前知识库目录，并设置基本步数上限和明确错误退出。
- [x] 从开始就分开传递系统规则、用户请求和工具结果，笔记中的指令性文字作为资料处理。限制单次读取量，超过上限时明确说明内容不完整；L03 再统一截断契约与继续读取方式。一次响应包含多个工具调用时先顺序处理，逐个关联结果；中途停止时保留未执行状态，不能编造执行结果。
- [x] 使用 Viper 读取 YAML 配置，通过结构体 `default` 标签和反射生成默认值；启动入口只加载配置并启动应用，不再接收 flags。
- [x] 使用 Gin 提供本地网页服务，前端页面独立放在 HTML 文件中，支持连续提问、处理中状态和错误提示。
- **完成后：** 能根据笔记完成一次回答；能说明用户消息、工具请求、工具结果、最终回答分别何时产生。暂时只有一个工具，不设计完整框架。

### L02：把单个工具扩展为工具注册表

L01 已能调用 read_note；接下来让同一个循环支持多个工具。

- [ ] 加入列目录和搜索工具，用工具名称到执行函数的注册表统一分发。循环只识别和分发请求，具体工具实现留在各自函数中。保留调用标识，未知工具返回明确错误，不按模型提供的字符串动态执行代码。
- **完成后：** 能连续完成搜索、读取、回答；增加工具只需注册，不复制一套 Agent Loop。

### L03：规范工具参数与返回结果

L02 已有多个工具，现在为它们建立共同的输入输出契约。

- [ ] 声明参数字段、类型、必填项和允许范围，分开结构与业务语义校验。统一工具结果中的状态、数据、错误信息、来源和截断标记。限制大输出，并提供继续读取的办法。
- **完成后：** 错误参数不会进入不适当的执行；模型能区分空结果、失败和内容不完整。已有工具全部遵守同一契约。

### L04：抽出统一工具执行器

L03 的校验、分发和结果处理已形成一段共同逻辑，将它从循环中抽出来。

- [ ] 建立 execute_tool 入口，集中完成工具查找、参数检查、实际调用、异常转换和结果包装。循环只调用执行器；工具函数只做自己的工作。此时保持直接调用结构，不提前开发完整插件系统。
- **完成后：** 所有工具都经过同一个执行入口；Loop、Executor 和工具函数的职责可以清楚区分。后续控制机制有统一接入位置。

### L05：加入运行状态与预算

L04 已有稳定循环和执行入口，现在定义一次任务的生命周期。

- [ ] 加入运行标识以及运行中、完成、失败、取消、预算耗尽等状态；把最初的步数上限扩展为运行预算，跟踪步骤、总耗时及可取得的 token/成本信息。明确终止后给用户返回什么，避免模型一句“完成”掩盖实际执行失败。
- [ ] 分开表达运行生命周期、任务完成情况和操作结果。一次运行结束后，任务仍可能部分完成或未完成；后续写入阶段还要列出已产生的变更与待核对操作。L10 增加等待确认状态，L12 增加操作结果未知，沿用同一套含义。
- **完成后：** 每次运行都能进入明确状态；耗尽预算后不继续发起操作；任务结果能区分成功和未完成。

### L06：加入超时、取消、主动暂停与运行中输入

L05 定义了预算和终态，本轮让执行中的模型或工具调用也受控。

- [ ] 设置单次调用截止时间，传播取消信号；对阻塞调用使用可取消接口或可终止的隔离执行方式。明确取消请求、执行已停止和结果待核对的区别；迟到结果不能自动启动下一步。
- [ ] 提供一个可控制延迟、失败和迟到返回的工具替身，用它验证取消后没有新操作、迟到结果不会推动循环；只增加必要的替换入口，无需建设测试平台。
- [ ] 让 Web 消息接收与 Agent 执行分开运行，增加会话内的消息收件队列和唤醒信号；上一轮未结束时也能接收追加消息，不必等最终回答。由同一个运行协调器串行接纳消息并更新任务，避免两个 Loop 同时推进同一任务。
- [ ] 区分主动暂停、追加要求、明确替换任务和取消。主动暂停先进入暂停请求状态，在安全边界停止推进后进入已暂停，收到继续指令才恢复；追加要求则唤醒当前执行、暂缓下一步、吸收新要求并修订计划后自动继续，保留尚未完成的原目标。状态询问只需回答，不默认取消或替换任务。
- [ ] 新消息到达时尝试中止可取消的模型调用或工具等待；无法安全停止的操作先标为待收尾或待核对，不立即启动与其冲突的新操作。已发出的模型请求不能原地修改输入，接纳新消息后要重新构建上下文、发起后续调用。为任务意图维护修订号，旧调用的迟到输出不能继续驱动旧计划；实际工具结果仍须记录和核对。
- **完成后：** 延迟工具不会无限拖住任务；取消后不启动新操作。当前仍为只读，后面引入写入和子任务时补充副作用与取消规则。

用延迟工具验证：上一轮未结束时补充要求可以被及时接收，后续按新要求继续且保留原目标；明确暂停后不会自动继续；连续追加的消息按接收顺序处理，旧回答不会覆盖新一轮结果。后续 L12、L17、L22 分别补充写入核对、持久恢复和后台作业的衔接，具体规则见第 6 节。

### L07：加入分级错误恢复

L03 已能表达错误，L05–L06 已能停止运行，在此基础上增加有边界的恢复。

- [ ] 区分临时网络故障、限流、参数错误、空结果和不可恢复错误。临时故障有限重试并退避，参数错误交回模型修正，空结果允许调整查询；重试共享原任务预算。先覆盖模型调用与只读工具，写操作在 L12 单独完善。
- [ ] 增加按固定序列返回工具调用、错误参数或最终回答的模型替身，配合工具替身复现恢复路径；检查重试次数、预算消耗和最终状态，不依赖真实模型恰好触发故障。
- **完成后：** 可以恢复的错误继续处理，持续失败明确结束；不会把每一种错误都当作重试理由。

### L08：建立结构化运行记录

L07 已经可能经历多步执行和恢复，接下来让完整过程可观察。

- [ ] 把早期控制台输出整理成结构化事件，关联运行、模型调用和工具调用，记录参数、结果或结果引用、错误、耗时及用量。记录模型与提示词版本并脱敏；使用实际事件定位行为，不依赖模型事后解释或私有推理。
- **完成后：** 能从记录中复原一次任务的可观察执行过程，定位错误发生在哪一步。这里是程序运行能力，无需为每节课手工写报告。

### L09：建立轻量评估基础

L08 提供了执行记录，本轮加上比较改动效果的办法，为后续演进提供参照。

- [ ] 从已用过的真实任务中选少量固定案例，保存可检查的期望。程序检查格式和工具行为，按明确标准人工检查语义结果；增加少量未用于日常调试的案例。以后随能力增长补充，不为每次小改动新建整套评估工程。
- [ ] 分开运行替身驱动的控制逻辑检查和真实模型的任务质量评估。初期检查回答依据、来源及资料冲突的处理；后续写入和多文件阶段复用第 7 节的内容质量案例，按已开放的能力增加检查。
- **完成后：** 能对比修改前后的任务结果并发现退步；模型有波动时重复运行并说明样本量。

### L10：建立权限策略并接入受限写入

只读运行基础已具备校验、停止、恢复、记录与评估，现在允许它产生受控文件副作用。

- [ ] 在执行器中加入允许、拒绝和等待确认的门控。声明可读写范围，规范化路径并考虑符号链接；资料内容不授予权限。先接入实验目录内创建草稿的工具，确认绑定具体操作与内容；范围内已有授权不反复询问。
- [ ] 等待确认时暂停依赖该操作的后续步骤；批准后重新检查操作内容与适用条件，拒绝后明确返回未完成事项。取消运行不会自动撤销已经成功的写入，结果中需列出已产生的变更。
- **完成后：** 未经允许的写入被程序拦截；拒绝不被当作暂时故障重试；任务能明确停在等待确认。暂不开放任意 shell。

### L11：建立变更提案与受控编辑

L10 已能在授权范围内创建草稿，本轮支持对已有文件做可检查的修改。

- [ ] 形成包含目标文件、原版本和局部改动的提案，展示差异。应用前核对版本，在可控制的写入方之间保护检查与写入的一致性；单文件使用适合存储的原子替换。保留撤销所需信息，撤销时也核对后续编辑。
- **完成后：** 修改可预览，过期提案明确冲突，原子替换避免读取到半份文件；并发覆盖与退出后的持久性分别验证，不由原子替换一并保证。

第一版的写入保证建立在所有程序写入都经过统一入口、遵守同一协调机制的前提下。应用期间约定外部编辑器不同时修改目标文件；发现版本变化时停止应用并报告冲突。普通本地文件上的“检查版本后替换”无法消除不遵守协调机制的外部写入竞争，不能承诺任意并发编辑下无覆盖。L18 的隔离副本用于生成和审阅变更，最终应用仍需核对目标版本并协调写入；需要更强保证时，再引入支持条件更新的存储或统一管理所有写入方。

### L12：补全写入幂等与结果核对

L11 已有明确的改动对象，现在把写操作纳入 L07 的恢复系统。

- [ ] 由程序为具体操作生成稳定标识，绑定提案、目标文件、基线版本和预期改动；持久保存预期前后状态和结果，重试沿用同一标识。工具超时或响应丢失时先核对执行记录与实际文件，再决定返回已有结果、重试或报告冲突。模型重新发出调用时，先核对是否引用已有操作；新的工具调用标识不代表新的写入意图，也不能仅凭内容相似合并两个独立请求。
- [ ] 使用故障入口模拟“文件已写成功，但结果未返回”，验证同一操作不会再次应用。无法判断是否执行时保留结果未知状态，停止依赖它的修改；不把未知当成失败后直接重试。
- **完成后：** 同一操作不重复追加内容；结果未知时不假定未执行。程序整体退出的恢复流程在 L17 接着完善。

### L13：统一构建模型上下文

已有工具、权限、运行状态和修改提案需要进入模型输入，本轮把输入组装显式化。

- [ ] 建立 Context Builder，区分稳定规则、当前请求、历史、工具定义、证据和任务状态。按需检索与读取，保留来源和版本，限制无关材料；根据真实漏检再尝试切分或语义检索，不默认引入向量数据库。
- **完成后：** 能检查一次模型调用实际包含哪些材料，以及为何包含它们；回答引用有依据，资料中的文字不会变成执行授权。

### L14：加入上下文预算与压缩

L13 已集中管理输入，现在可以在同一位置控制上下文增长。

- [ ] 先限制工具输出、去掉可重新读取的冗余，再按需要压缩旧历史。独立保留目标、约束、提案、操作状态和证据指针；摘要不能替代程序权限，工具请求与结果关系必须仍然合法。
- **完成后：** 长任务能在上下文预算内继续；关键要求与来源可恢复；压缩失败有明确退路。

### L15：保存与恢复会话

L13–L14 已明确哪些信息构成当前会话，本轮将会话从内存放入持久存储。

- [ ] 保存消息、会话标识、提案、等待确认事项及授权作用范围，规定记录格式。恢复时检查不完整工具调用、过期提案和会话归属；先恢复对话与可见状态，不自动重做中断中的写操作。
- [ ] 保存运行中追加的消息、消息标识、接收顺序和已接纳位置，以及当前意图修订号与暂停状态。恢复后未接纳的补充不能丢失，已经接纳的消息不能重复改变任务；消息已收到与已用于执行分别可见。
- **完成后：** 重启后可以继续交流和审阅；会话不串线，旧授权不扩大。能区分“找回聊天”和“恢复实际执行”。

### L16：加入任务状态、固定 Workflow 与动态计划

已有会话可以承载持续交互，本轮把多步工作的进度从聊天文本里提取出来。

- [ ] 先做轻量 Todo，再为有依赖的工作增加任务关系；每项写清输入、完成条件与产物。允许修订计划，完成状态与工具结果或产物检查关联；程序检查文件、链接和修改范围，语义保留按明确标准检查。
- [ ] L16a：实现程序控制的固定 Workflow。用“检索资料 → 生成提案 → 检查 → 必要时修订 → 授权检查 → 应用”贯穿演示，定义节点输入输出、执行顺序、条件分支、失败出口和等待确认。检查不通过时只能进入修订或结束，不能跳到应用；已有授权可通过门控，无需重复询问。
- [ ] L16b：在固定流程中实现 Executor 与 Refiner 的职责。Executor 生成或执行具体工作，Refiner 根据明确反馈修改提案，再交回检查节点；设置修订次数、总预算和无进展退出条件。先由同一 Agent 运行中的不同步骤承担，独立角色实例留到 L24。
- [ ] L16c：加入 Planner，根据目标产生结构化步骤、依赖和完成条件，执行结果或用户补充到来时可以修订计划。程序检查依赖是否合法、工具是否可用、权限是否允许，再交给执行流程。固定 Workflow 与动态计划分别提供运行入口，复用任务状态与执行控制。
- **完成后：** 能分别运行固定流程和动态计划；能查看已完成、待做和受阻步骤；依赖未满足不会执行；模型打勾不能代替实际完成。

按 L16a → L16b → L16c 顺序完成。同一资料整理任务分别走两种入口；固定流程验证检查失败、修订上限与等待确认，动态计划验证中途追加“只生成提案，不写入”后计划更新且没有新写入。这里先实现流程和职责，不提前创建多个 Agent。

### L17：持久化任务并恢复中断执行

L12 有操作记录、L15 有会话、L16 有任务状态，现在把它们连接成恢复流程。

- [ ] 持久化任务状态和检查点；启动恢复时核对计划、操作记录与真实文件，处理写成功但完成标记没保存的间隙。恢复预算、取消和等待确认状态；先处置不确定操作，再继续剩余步骤。
- [ ] 同时保存固定 Workflow 的定义版本、节点结果、分支与修订次数，以及动态计划的版本和执行位置；重启后沿原运行方式核对恢复，已完成节点不因恢复而重新产生副作用。
- [ ] 将已接纳消息的位置、意图修订号与更新后的任务状态一致地持久化。恢复主动暂停的任务时保持暂停，继续前重新检查文件基线、授权范围和待核对操作；暂停期间不消耗执行时长预算，已消耗的步骤与 token 不清零，绝对截止时间及授权有效期仍需检查。
- **完成后：** 退出后可以继续或受控结束；已执行的不重复，未执行的不遗漏；无法确认的操作明确待处理。

### L18：支持多文件变更与隔离工作区

L11–L12 的单文件写入和 L17 的恢复流程已经成立，再把它们扩展到一组相关文件。

- [ ] 在工作副本或 Git worktree 中生成完整变更集合，先验证内容和链接，再进入受控应用流程。检查目标基线，记录应用进度及恢复/补偿步骤；恢复期间限制新修改。明确多个文件替换并不天然构成事务。
- **完成后：** 多文件任务中断后能恢复到一致状态或完成剩余应用；冲突可见；隔离工作区中的未交付成果不会被随意清理。

### L19：加入可管理的长期记忆

会话与任务状态已有各自存储，现在单独处理需要跨任务保留的信息。

- [ ] 先保存用户明确要求记住的偏好，注明来源、作用域与更新时间，提供查看、修改、删除。区分 Wiki 内容、聊天记录、任务进度和偏好记忆；再根据使用效果决定是否自动提取。
- **完成后：** 临时要求不永久污染行为，删除和更新有效；记忆不覆盖当前请求或硬性权限。

### L20：加入 Skills 与按需加载

L13 管理上下文，L19 管理长期信息，本轮把可复用的任务流程独立组织。

- [ ] 把已经有效的资料入库、术语整理等流程写成技能，包含适用范围、步骤、资源与检查方式。常驻简短目录，按需加载正文；记录触发和版本，区分技能知识与可执行工具。
- [ ] 区分 Skills 的文字指导与 L16 的程序流程：需要强制执行的节点顺序、分支和授权检查由 Workflow 承担，不能仅靠技能正文要求模型遵守。后续角色按任务加载相应 Skills。
- **完成后：** 相关任务可以复用流程，无关任务不加载全部规则；技能不能扩大权限，变更效果可以比较。

### L21：加入 Hooks 扩展点

执行器、观测、上下文、记忆与技能都已有具体实现，通用扩展的位置已经清楚，本轮再抽象扩展接口。

- [ ] 围绕请求进入、工具执行前后、任务结束等事件开放 Hooks。明确执行顺序、失败策略和可修改范围；把适合的记录与检查接入，但强制权限和预算不可被普通扩展绕过。
- **完成后：** 增加通用行为无需到处改核心代码；工具成功后某个后置扩展失败，不会被错误地解释成应重新写入。

### L22：引入后台作业、事件处理与本地 Web 界面

已有任务状态、取消、恢复与扩展点，接下来让慢操作可以离开前台同步执行。

- [ ] 用作业标识、状态和事件队列管理后台工作，限制并发与排队；完成通知关联原任务，处理重复事件、取消竞争和迟到结果。空闲时等待事件，不让模型不断轮询。
- [ ] 把 L06 的用户消息、暂停和继续信号接入同一事件处理流程；新消息能唤醒后台等待。按新要求保留仍然有效的作业，对已过时的作业发出取消或暂停请求；不能暂停的作业明确显示为仍在执行，其结果进入待核对状态，不能自动推动旧计划。作业记录所基于的意图修订号，由协调器检查结果是否仍适用。
- [ ] L22a：完善本地服务层，提供创建任务、追加消息、查询状态、暂停、继续、取消和确认提案的接口。Web 页面通过同一服务层和权限检查操作任务，同一任务只由一个协调器推进。服务先绑定本机地址，浏览器端不保存模型密钥；校验访问来源与会话归属，页面内容和浏览器自动化不能绕过授权接口修改 Wiki。
- [ ] L22b：实现 Web 对话与任务页面，用事件流展示回答、工具活动、任务状态及消息接纳情况；上一轮未结束也可发送补充。提供暂停、继续、取消，以及提案差异、批准和拒绝操作；区分“暂停请求已接收”与“执行已暂停”。
- [ ] L22c：支持刷新和连接中断后的状态同步，用事件序号续接或重新获取快照，并对重复提交去重。关闭页面只断开观察连接，后台任务按既定策略继续；明确取消才停止任务。L24–L25 在同一界面增加父子任务、角色实例和产物视图。
- **完成后：** 提交与完成明确区分；用户可通过本地网页使用 Agent；运行中仍可追加消息；刷新不重复创建任务，任务结果不串线，取消和工作者退出有确定处理。

先完成后台事件处理，再依次完成 L22a → L22b → L22c。验收“网页提交整理任务 → 执行中补充只生成提案 → 暂停 → 刷新 → 继续 → 审阅差异”，检查前后端状态一致、补充没有丢失，且没有未经授权的新写入。

### L23：实现网页观察、浏览器交互、远程浏览器与 MCP

内部工具和 Web 任务入口已具备统一控制机制，本轮让 Agent 操作资料网站。所有新增工具从接入时就经过统一执行器，按任务限定访问站点与动作范围，重定向后也检查目标。先实现本地浏览器，再接入远程浏览器与 MCP；各阶段都实际运行，浏览器会话本身也是需要管理和恢复的执行资源。

- [ ] L23a：实现 HTTP 网页读取适配器，提取正文并记录最终 URL、标题、抓取时间和内容摘要或版本。处理重定向、失败和内容截断，将网页文字作为不可信资料接入 L13；用一个真实公开资料源生成带引用的 Wiki 提案。
- [ ] L23b：用 Playwright 实现本地浏览器适配器，提供打开页面、列出标签页、观察页面和截图工具。观察结果包含 URL、页面标识、可见文本与可操作元素，控制输出大小；先用角色、名称等语义定位，再增加截图观察与基于视觉位置的操作，绑定截图时的页面及视口状态，页面变化后重新观察。
- [ ] L23c：实现点击、输入、滚动、翻页和必要的等待，形成“观察 → 决策 → 动作 → 再观察 → 验证”的循环。定位依据必须来自实际观察，目标不唯一或观察过期时重新定位；点击返回成功不代表业务操作成功，需检查目标页面、结果列表或保存反馈。用支持动态加载、搜索和分页的本地测试资料站，验证 Agent 自行选择动作，而非仅运行写死的浏览脚本。
- [ ] L23d：管理浏览器会话、标签页归属、登录状态、超时、暂停与断线恢复。恢复前重新观察页面并核对操作结果；浏览器进程丢失时按已保存的任务和来源重建必要页面，不把保存聊天当成保存了实时页面。提交表单等有副作用的动作经过权限门控；提交超时先核对是否生效，没有可核对依据时停止自动重试。用测试站模拟保存成功但响应丢失、页面变动与中途暂停。
- [ ] L23e：接入 Browserless 作为远程浏览器运行服务，复用浏览器适配器的观察与动作契约；保持本地与远程两种入口，处理连接认证、会话过期、断线、并发配额及资源释放。使用自建或托管服务均可，实际跑通同一任务，不用模拟调用替代远程接入验收。登录状态、连接凭据与日志分别管理，避免将凭据送入模型上下文。
- [ ] L23f：将网页工具接入统一执行器，再通过 MCP 发现和调用一组浏览器工具，对照直接适配与 MCP 接入的差异。两种入口都执行参数校验、权限、超时、记录与结果核对，处理认证、连接和工具定义变化；L24–L25 让 Searcher 实例实际使用网页工具返回证据，给不同工作者分配独立浏览器会话，避免互相导航或覆盖输入。
- **完成后：** Agent 能读取静态网页、观察动态网页并执行有反馈的交互；本地 Playwright 与远程 Browserless 均通过实际任务验证；浏览器操作沿用现有授权、暂停与恢复机制，网页资料有可追溯来源。

按 L23a → L23b → L23c → L23d → L23e → L23f 顺序完成。贯穿任务是“在资料站搜索缓存文章，翻页收集两篇相关内容，生成带来源的 Wiki 提案”。本地测试站分别验收结构化观察和截图驱动的交互，以及加载延迟、元素变化、暂停继续、提交结果未知；本地和远程浏览器分别跑通同一任务。Agent 的 Web 界面显示当前访问页面、动作状态、必要截图与结果，供用户检查。

### L24：建立 Agent 管理与固定角色的多 Agent Workflow

L16 已有固定 Workflow 和角色职责，单 Agent 已具有完整上下文、状态和执行控制。本轮依次实现子 Agent 基础、角色定义与实例管理，再把固定流程中的角色拆成独立 Agent。

- [ ] L24a：实现创建、等待和回收子 Agent 的统一入口。为子 Agent 创建独立上下文，规定输入、工具子集、预算、输出、证据和未解决项；关联父子运行，传播暂停、取消和总预算。先用只读调查任务验证委派，由主流程统一应用修改。
- [ ] L24b：建立 Agent 定义注册表与运行实例管理。定义保存角色职责、系统提示词、模型配置、工具范围、Skills、输入输出契约和版本；实例保存运行标识、父任务、所用定义版本、独立上下文、状态、预算和产物。同一定义可创建多个实例，运行中的实例固定配置版本，配置更新不会悄悄改变已有实例。
- [ ] L24c：实现第 6 节列出的 Planner、Searcher、Executor、Reviewer、Refiner、Router 角色。将 Planner → Searcher → Executor → Reviewer → Refiner（检查未通过时）→ Reviewer 接成预定义流程，检查通过后由主流程执行授权检查与应用；每个业务角色用独立 Agent 实例处理自己的输入输出，流程顺序和修订上限由程序控制。固定流程中的 Planner 只细化当前阶段的工作内容，不改变预定义角色连接关系。
- [ ] L24d：加入 Router，先用程序规则区分问答和整理任务，再实现模型输出结构化路由结果的方式。候选目标限定为已注册的单 Agent 或固定 Workflow，程序校验选择，无匹配时明确澄清或返回可处理范围。保留显式选择入口，避免路由结果让必做流程无法被独立验证；L25 再增加动态委派目标。
- [ ] 在 Web 界面查询实例列表、父子关系、当前步骤、预算及结果，支持查看指定实例、暂停、继续、取消和失败后受控重试。子任务失败或取消时父流程按依赖规则阻塞、降级或结束，不能把缺失结果当成功；创建和重试均受权限、总预算、并发及递归深度约束。
- [ ] 将实例管理同步接入 L22 的 Web 界面，展示父子任务、角色、当前步骤、预算与产物；让 Searcher 用 L23 浏览器工具收集带来源的证据，验证独立会话归属与父任务暂停、取消时的浏览器资源处理。
- **完成后：** 固定角色流程可真实运行，各角色有独立实例与上下文；定义和实例可分别查询；子任务权限不超过父任务；路由、检查与修订可追踪，没有无限递归委派。

按 L24a → L24b → L24c → L24d 顺序实现并保留前一步能力。用“根据指定三篇缓存笔记生成一篇整理提案”验收固定团队，观察证据、提案、检查反馈与修订稿逐步交接；模拟一次检查失败和一次子 Agent 超时，确认修订及父任务终态正确。单独验证 Router 的规则和模型两种实现，并与 L16 的单 Agent 流程比较质量、时间和成本。

### L25：实现运行时动态委派、多 Agent 协作与并发控制

L24 的固定角色团队已可运行，L18 已能隔离变更。本轮在保留固定模式的基础上，由主 Agent 在运行时决定任务拆分、所需职责与实例数量，再逐步开放并发和隔离修改。

- [ ] L25a：实现主 Agent 动态编排。主 Agent 先调查任务范围，再输出结构化委派计划，包含子任务目标、依赖、输入、产物、验收条件、角色和预算；程序校验后创建实例。既支持选择 L24 的角色模板，也支持为通用工作者临时定义职责；临时定义同样保存版本并受已有权限边界约束。先顺序执行动态产生的子任务，允许根据结果新增、合并或撤销未完成子任务。
- [ ] L25b：加入并发执行与协作协议。增加原子认领、可回收所有权、结构化消息、请求标识、超时和停止协议；只有依赖满足的任务可被领取。限制活跃实例数和总预算，避免多个子任务分别花完父任务的剩余预算；重分配后拒绝旧工作者提交，重复消息不能导致重复执行。
- [ ] L25c：允许工作者在独立工作区生成修改，主流程集中验证和合并。管理工作区、任务基线、产物归属和冲突，结果接纳需核对任务所有权及意图修订号。失联任务可接手，未交付成果保留；工作者不能绕过主流程直接写入共享 Wiki。
- [ ] L25d：将动态委派加入 Router 候选，并支持显式选择固定或动态模式。沿用 L06 的运行中输入：用户补充后主 Agent 更新计划，将新约束传给相关子任务，暂停或取消失效子任务，核对已发生的副作用；旧修订下的结果只能在重新检查适用性后接纳。明确主 Agent 是面向用户的协调入口，工作者通过结构化消息汇报。
- **完成后：** 同一系统中固定角色 Workflow 与动态委派均可独立运行；动态模式能按任务规模创建、调整和结束实例；没有重复认领导致的重复写入或静默覆盖，失联工作可接手，旧工作者不能覆盖新结果。

按 L25a → L25b → L25c → L25d 顺序实现。用“调查整个缓存专题的重复、矛盾和断链”验收：先动态拆成调查任务，再依据发现追加受影响页面的检查；小任务可以不委派，大任务必须实际展示多个实例与结果汇总。随后模拟工作者失联、重复消息、旧结果迟到和用户中途追加“只给建议，不修改”，检查接管、预算、暂停恢复和写入范围。另用同一固定资料集分别运行 L24 与 L25，保留两种模式的验收结果，不要求动态模式必然更快或更好。

### L26：加入定时触发与持续维护

前台、后台和协作任务都复用同一运行基础，调度器只需要负责产生任务。

- [ ] 记录调度触发标识，定义去重、重叠、错过调度、暂停和预算策略；任务执行仍走现有 Runner。无人确认时保持待审阅，只有有意义变化或失败时通知。
- **完成后：** 重复触发不重复修改，任务不会无限积压；调度可暂停，任务与结果可查询。

### L27：优化质量、延迟与成本

主要能力已构建完成，利用 L08–L09 持续积累的记录和评估来优化。

- [ ] 找出主要耗时与成本，再分别尝试缓存、减少无关上下文、模型选择或受限并发，一次比较一个因素。缓存关联资料版本和权限范围；过载有排队或受控拒绝策略。
- **完成后：** 改进经过质量、延迟、成本联合检查；旧能力不因提速悄悄退化，效果波动和适用范围可说明。

### L28：完成集成验证与交付

最后验证累计构建的机制能否协同工作，并准备可持续使用的运行方式。

- [ ] 选择代表性 Wiki 任务，检查权限、取消、上下文、写入、恢复与并发的交互。按需组合限流、响应丢失、人工编辑与进程退出等条件。整理实际需要的配置、启动、备份、升级与恢复说明，明确发布检查。
- [ ] 分别验收单 Agent 的固定 Workflow、动态计划、固定角色多 Agent 和运行时动态委派；验证 Router 的选择、Reviewer–Refiner 循环的退出条件及 Agent 实例管理。固定与动态多 Agent 都必须独立跑通，并覆盖运行中追加消息、暂停和中断恢复；自动路由的一次成功不能代替各模式验收。
- [ ] 从 Web 界面提交包含网页资料收集的任务，覆盖浏览器交互、证据引用、提案审阅与受控写入。验证页面刷新、事件重连、本地及远程浏览器中断、多 Agent 浏览器会话隔离；交付说明包含本地服务启动、浏览器依赖、远程连接配置及会话资源清理。
- **完成后：** 从干净环境可以运行；任务终态和文件结果一致；关键约束与预设质量要求通过；已知限制清楚。无需把每轮教学材料归档成套。

## 5. 前几轮怎样在同一份代码上演进

| 位置 | L01 | L02 | L03 | L04 |
|---|---|---|---|---|
| 核心循环 | 识别调用并直接执行唯一工具 | 从注册表选择工具 | 调用前校验、返回后统一格式 | 只把请求交给执行器 |
| 工具定义 | 一个 read_note | 多个工具及注册关系 | 各工具有明确参数与结果契约 | 契约继续沿用 |
| 共同逻辑 | 简单写在循环附近 | 出现查找和分发 | 增加通用校验与错误包装 | 收拢到 execute_tool |
| Wiki 演示 | 读取并回答 | 搜索后读取并回答 | 同一任务，调用更规范 | 同一任务，结构更清楚 |

L05 开始，运行状态与预算接到这个结构上；L06 将超时和取消接到模型调用及执行器；L07 在统一错误表达和停止条件之上加恢复。

这种演进不要求每一轮都增加用户功能，也不需要先破坏旧代码。每次在已有运行结构上增加一块明确的职责。

## 6. 中后期的几个关键衔接

**执行恢复的衔接**

L07 先处理模型与只读工具的可重试故障。L10 引入写权限，L11 形成具体修改提案，L12 才针对已经存在的写操作加入幂等与结果核对。L17 把操作记录与会话、任务状态连接起来，处理进程退出后的恢复。

因此不会在尚未有写入工具时，先要求理解一整套写事务；也不会把所有错误恢复混在一轮中。

**上下文与状态的衔接**

L01–L03 先建立消息角色、资料边界与输出限制，L13 再把输入组装集中到 Context Builder，L14 才有集中的压缩入口；L15 保存会话，L16 把工作进度独立成任务状态，L17 再恢复实际执行。聊天历史、任务状态和真实文件结果各有职责。

运行状态与任务结果沿用 L05 的区分。例如，修改三个文件时已成功两个，第三个结果未知，此时取消：运行先记录取消请求，确认执行停止后进入已取消状态；任务至少部分完成，两个成功变更保留，第三个待核对。L17 恢复时先核对未知操作，保留取消决定，不自动继续剩余修改；L22 的后台作业也遵循这一规则。

**主动暂停与运行中追加消息的衔接**

这项能力由运行协调器、消息收件队列、唤醒与中断信号共同承担。L06 先在前台实现，L15 保存消息与接纳进度，L17 连接检查点，L22 扩展到后台作业；无需另起一套 Agent Loop。

典型过程：用户要求“整理缓存相关的三篇笔记”，上一轮仍在执行时追加“保留原文，只增加交叉链接”。输入端立即接收并唤醒协调器；协调器暂缓旧计划，记录新消息、更新意图修订号，并按新约束继续原任务。尚未应用且不符合新要求的提案失效；已经成功的写入要如实报告，必要时另行形成撤销或修正提案；结果未知的写入先按 L12 核对。不能把打断执行等同于撤销副作用。

若用户说“先暂停”，则在可安全停下的位置保存进度，等待明确的继续指令。若说“取消”，则结束当前执行。普通补充沿用原任务目标和剩余预算；只有明确要求放弃或替换任务时才改变原目标，含义不清且影响写入范围时先澄清。运行中的状态询问可以立即回答，任务继续推进。

验证至少覆盖：模型生成中追加要求、工具等待中暂停、连续两次修改要求、旧后台结果迟到，以及暂停后重启再继续。检查消息不丢失、不重复接纳、同一任务没有两个执行循环并行推进；在系统接纳补充之后，不再依据已失效的旧计划启动新操作。后台仍在执行的操作和已经发生的副作用单独展示，不承诺任意操作都能立即停止。

**扩展接口的衔接**

L04 先得到一个直接、清楚的执行器。权限、记录、上下文等逻辑逐步接入之后，在 L21 增加 Hooks。先有值得扩展的具体行为，再定义抽象扩展接口，不让初学阶段陷入框架设计。

**协作的衔接**

L16 先实现单 Agent 下的固定 Workflow、执行与修订职责，再实现动态计划；L18 学会隔离、验证和应用多文件变更，L22 学会管理后台状态与取消。L24 先建立子 Agent 与定义、实例管理，将既有职责接成固定角色团队；L25 再实现运行时动态拆分与创建、并发协作和隔离合并。这些阶段按顺序实际完成，固定与动态两种多 Agent 模式持续保留。

固定模式预先规定角色连接关系和分支，运行时按流程创建实例；动态模式在运行时由主 Agent 根据任务和已有结果决定拆分方式、职责、依赖与实例数量。角色模板可以共用，但只让 Router 在几个固定流程之间选择，还不算实现了动态委派。

| 角色 | 输入与输出 | 首次实现及后续演进 |
|---|---|---|
| Planner | 目标、约束和反馈 → 步骤、依赖与完成条件 | L16 动态计划；L24 固定团队内细化工作；L25 由主 Agent 承担动态拆分 |
| Searcher | 查询范围和问题 → 证据、来源和信息缺口 | L02 的搜索工具与 L13 的证据管理；L24 独立检索实例 |
| Executor | 计划步骤和证据 → 提案或受控执行产物 | L16 执行步骤；L24 独立实例生成提案；L25 在隔离工作区执行 |
| Reviewer | 产物与验收标准 → 检查结果、问题及依据 | L09 评估基础；L16 检查节点；L24 独立审核实例 |
| Refiner | 原产物和检查反馈 → 修订产物与问题处理说明 | L16 有界修订循环；L24 独立修订实例 |
| Router | 用户请求和可用处理方式 → 合法目标或澄清结果 | L24 规则路由与模型路由；L25 增加动态委派目标 |

Executor Agent 负责完成任务步骤，L04 的工具执行器负责参数校验和实际调用，两者职责不同。Reviewer 的语义判断需要证据，程序检查与授权门控仍由 Harness 强制执行。角色定义描述可复用能力；实例表示某次实际运行，多个实例可以使用相同角色定义，不要求使用不同模型或不同操作系统进程。

## 7. 功能范围与 Harness 的关系

| 用户功能 | 用来承载的学习内容 |
|---|---|
| 读取、搜索与问答 | Loop、工具契约、执行控制、上下文与证据 |
| 创建草稿、补充旧笔记 | 权限、变更提案、版本保护、幂等 |
| 跨页面整理、维护引用 | 计划、验证、多文件一致性 |
| 批量整理与恢复 | 持久任务、检查点、后台作业 |
| 通过本地网页使用 Agent | 服务接口、事件流、运行中输入、暂停继续和提案审阅 |
| 浏览网页并收集资料 | HTTP 读取、Playwright 观察与交互、Browserless 远程运行、证据与会话管理 |
| 专题协作和定期维护 | 子任务契约、并发协调、调度 |

这些任务只提供实际操作对象。课程标题和迭代单位始终是正在建设的 Harness 机制。

内容质量用少量真实案例贯穿验证，随能力开放逐步加入：

| 案例 | 检查标准 | 开始使用 |
|---|---|---|
| 根据笔记回答问题 | 关键结论有可定位的来源，缺少依据时说明不确定性 | L09 |
| 处理内容相互冲突的资料 | 标注分歧及来源，不擅自把其中一方当作确定事实 | L09；写入后继续复用 |
| 补充已有笔记 | 保留原有关键事实和来源，只修改请求范围内的内容 | L11–L12 |
| 合并两篇笔记 | 在任务完成条件中列出需要保留的事实与引用，完成后逐项核对 | L16–L18 |
| 调整标题或移动页面 | 检查受影响的内部链接与标题锚点，更新引用或明确报告未处理项 | L18 |

文件、链接和修改范围由程序检查；事实保留、依据是否充分与表达是否准确按明确标准人工检查。L16 将这些要求写入具体任务的完成条件，L18 在多文件应用前验证完整变更集合。通过文件写入检查不等于通过内容质量检查。

## 8. 怎样使用这份清单

每次选当前轮次，先看看它承接了哪些代码，再理解并实现本轮机制。完成后用相关输入确认效果即可。

不必机械执行统一教学模板。有的轮次以讲解和写代码为主，例如 L01；有的轮次需要对照不同实现，例如 L03；有的轮次更适合加一个模拟超时或中断，例如 L06、L17。

无需每轮保存独立版本、维护失败样例目录或提交实验报告。若需要回退，可以在较大改动后做普通 Git 提交。进入评估、观测和恢复相关课程时，再建设相应的程序能力。

验证随着项目成熟逐步深化：

- 早期：运行几个明确的任务，查看调用和结果。
- 中期：复用固定案例，检查权限、修改范围、失败和恢复。
- 后期：组合上下文、写入、取消、并发等机制，验证它们相互配合。
- 语义效果与程序约束分别检查；对模型波动进行必要的重复评估。

## 9. 参考资料与课程映射

- [Agent Learning Hub](https://datawhalechina.github.io/Agent-Learning-Hub/)及同仓库 [学习清单](https://github.com/datawhalechina/Agent-Learning-Hub#learning-todo-list)：用于检查知识覆盖。
- [Learn Claude Code 学习路径](https://learn.shareai.run/zh/timeline/)：参考其逐步增加 Harness 机制的教学方式。已查阅关键章节；s20 正文未能加载，仅参考时间线中的综合集成定位。
- 顺序、Wiki 示例和阶段边界为本项目重新编排；不把教学示例或本文顺序声称为成熟产品内部实现。
- [Playwright 元素定位](https://playwright.dev/docs/locators)与 [Browserless 概览](https://docs.browserless.io/overview/intro)：用于 L23 的浏览器控制与远程运行学习。实现时核对所选版本支持的接口和连接方式。

| 参考主题 | 本路线 |
|---|---|
| s01 Loop、s02 Tool Use | L01–L04 |
| s03 Permission | L10 |
| s04 Hooks | L21 |
| s05 TodoWrite、s12 Task System | L16–L17 |
| s06 Subagent | L24 |
| s07 Skills | L20 |
| s08 Context Compact | L14 |
| s09 Memory | L19 |
| s10 System Prompt | L13 |
| s11 Error Recovery | L07；写操作在 L12 补全 |
| s13 Background Tasks | L22 |
| s14 Cron Scheduler | L26 |
| s15–s17 Teams / Protocols / Autonomy | L25 |
| s18 Worktree Isolation | L18，并在 L25 复用 |
| s19 MCP | L23 |
| 时间线 s20 综合集成 | L28 |

工具契约、统一执行器、运行预算、超时取消、评估、写入幂等和崩溃核对被放入相应构建阶段，使每一步都有已存在的代码可以承接。
